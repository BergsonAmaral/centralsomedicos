// Supabase é configurado automaticamente pelo módulo @nuxtjs/supabase.
// Este plugin existe para inicializações client-side adicionais se necessário.

export default defineNuxtPlugin(() => {
  // useSupabaseClient() e useSupabaseUser() estão disponíveis via auto-import
  // em qualquer componente ou composable sem necessidade de configuração extra.
})
